{
  "scripts": {
    "deploy": "gh-pages -d dist",
    "vercel-build": "parcel build src/index.html",
    "build": "rm -rf .parcel-cache dist && parcel build src/index.html",
    "dev": "parcel src/index.html --public-url /"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.13.3",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "process": "^0.11.10"
  },
  "staticFiles": {
    "staticPath": "assets"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
